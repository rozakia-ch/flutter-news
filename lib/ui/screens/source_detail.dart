import 'package:flutter/material.dart';

class SourceDetail extends StatelessWidget {
  const SourceDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
